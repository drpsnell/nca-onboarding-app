# NCA Onboarding App

Project-specific context for the NCA Onboarding web application.

> **Note:** NCA methodology, clinical references (Red/Yellow Flags), and library documentation are in the user-level `~/.claude/CLAUDE.md` and available across all projects.

## Project Overview

Next.js application for onboarding clinicians to the NeuroCentric Approach methodology.

## Tech Stack

- **Framework:** Next.js (App Router)
- **Database:** Prisma
- **Styling:** (TBD based on project files)

## Project Structure

```
src/
├── app/
│   ├── admin/      # Admin routes
│   ├── api/        # API routes
│   └── page.tsx    # Main page
├── components/     # React components
├── data/          # Data files
└── lib/           # Utilities
prisma/            # Database schema
```

## Reference Documents

- **PACE Policy & Procedure Manual** — `docs/PACE-Policy-Procedure-Manual.txt` (OCR'd text, 102 pages, from original PDF)
